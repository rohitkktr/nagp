apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: nagp
  labels:
    app: cart-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      imagePullSecrets:
        - name: docker-credentials
      containers:
      - name: cart-service
        image: YOUR_DOCKER_USERNAME/nagp-cart-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8002
          protocol: TCP
        env:
        - name: CART_PORT
          value: "8002"
        - name: CART_HOST
          value: "0.0.0.0"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /docs
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /docs
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 5
